# CI and packaging scripts pass --no-daemon/--max-workers=1 explicitly where a
# hermetic build matters; these properties therefore only shape local builds.
org.gradle.caching=true
org.gradle.configuration-cache=false
org.gradle.jvmargs=-Xmx1536m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
